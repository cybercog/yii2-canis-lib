<?php
namespace infinite\web;

class AssetBundle extends \yii\web\AssetBundle
{
	/**
	 *	@inheritdoc
	 */
	// public function init()
	// {
	// 	parent::init();
	// 	if (!isset($this->publishOptions['forceCopy']) && defined('YII_ENV') && YII_ENV === 'dev') {
	// 		$this->publishOptions['forceCopy'] = true;
	// 	}
	// 	var_dump($this->publishOptions);
	// 	var_dump(YII_ENV);
	// }
}